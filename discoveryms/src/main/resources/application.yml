spring:
  application:
    name: eureka-server

eureka:
  instance:
    hostname: eureka-server
  server:
    enable-self-preservation: false
  client:
    register-with-eureka: false
    fetch-registry: false

server:
  port: 8761
